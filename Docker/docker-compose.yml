version: '3.4'
services:
  rstudio:
    build:
      context: .
      dockerfile: Dockerfile
    image: docker.io/znmeb/rstudio
    environment:
      - POSTGRES_PASSWORD
    ports:
      - 8787:8787
  postgis:
    image: docker.io/znmeb/postgis
    environment:
      - POSTGRES_PASSWORD
    ports: 
      - 5439:5432
